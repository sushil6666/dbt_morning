version: 2

# ---------------------------------------------------------------------------
# Intermediate model docs + unit tests
# ---------------------------------------------------------------------------
# All three models are materialized as ephemeral, so they have no physical
# DB objects and cannot be queried directly. Unit tests let us validate
# their SQL logic in isolation using pre-defined fixture data.
# ---------------------------------------------------------------------------

models:

  - name: int_daily_revenue
    description: >
      Combines ticket revenue and in-park sales revenue by visit date using a
      FULL OUTER JOIN so dates that appear in only one source are still included.
      All NULL sides of the join are coalesced to 0.

      Grain: one row per `visit_date`. Includes all dates that appear in either
      `stg_sales__tickets` or `stg_sales_transactions__sales_transactions`.
    columns:
      - name: visit_date
        description: Calendar date of the park visit.
        data_tests:
          - not_null
          - unique
      - name: ticket_revenue
        description: Sum of ticket `final_price` for the visit date.
      - name: in_park_revenue
        description: Sum of POS `total_amount` for the visit date.
      - name: food_revenue
        description: Portion of in-park revenue where `category = 'food'`.
      - name: merch_revenue
        description: Portion of in-park revenue where `category = 'merchandise'`.
      - name: total_revenue
        description: Combined ticket and in-park revenue for the visit date.
      - name: unique_visitors
        description: Distinct customers with a ticket for the visit date.
      - name: tickets_sold
        description: Total ticket rows for the visit date.


  - name: int_ride_metrics
    description: >
      Ride-level feedback enrichment built from the ride catalog and ride-specific guest
      feedback. One row per `ride_id`.

      This model filters feedback to `category = 'ride'` and `ride_id IS NOT NULL` so the
      downstream ride marts are based only on actual ride feedback. It also carries ride
      attributes that matter for stakeholder analysis, including status, min height, and
      opened date, plus review coverage signals such as `has_reviews` and `review_count_band`.
    columns:
      - name: ride_id
        description: Natural primary key from `stg_park_assets__rides`.
        data_tests:
          - not_null
          - unique
      - name: ride_name
        description: Display name of the ride.
      - name: ride_type
        description: Physical ride category.
      - name: min_height_cm
        description: Minimum rider height requirement in centimetres.
      - name: thrill_level
        description: Operations-assigned thrill intensity.
      - name: zone
        description: Park zone where the ride is located.
      - name: is_haunted
        description: Haunted ride flag.
      - name: status
        description: Current ride operating status.
      - name: opened_date
        description: Date the ride first opened to the public.
      - name: capacity_per_hour
        description: Maximum riders per hour under normal conditions.
      - name: avg_wait_minutes
        description: Rolling average wait time in minutes.
      - name: total_reviews
        description: Count of ride-category feedback rows linked to the ride.
      - name: avg_rating
        description: Average satisfaction rating from ride feedback.
      - name: positive_reviews
        description: Count of positive ride feedback rows.
      - name: neutral_reviews
        description: Count of neutral ride feedback rows.
      - name: negative_reviews
        description: Count of negative ride feedback rows.
      - name: positive_review_pct
        description: Percentage of ride feedback rows classified as positive.
      - name: has_reviews
        description: TRUE when the ride has at least one ride feedback row.
      - name: review_count_band
        description: Review volume band for the ride.
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['No Reviews', 'Single Review', 'Low Volume', 'Established']


  - name: int_customer_visits
    description: >
      Customer visit-level enrichment built from ticket purchases, matched POS spend,
      and matched visitor feedback.

      Grain: one row per `ticket_id`.

      POS spend and feedback are matched on `(customer_id, visit_date)`. When no
      matching POS transaction exists, `in_park_spend` is set to 0. When no matching
      feedback exists, `avg_rating` remains NULL and `has_feedback` is FALSE.

      This intermediate model centralizes reusable visit behavior fields so downstream
      customer marts do not need to re-derive booking lead time, discount usage, or
      feedback participation.
    columns:
      - name: ticket_id
        description: Visit-level primary key sourced from `stg_sales__tickets`.
        data_tests:
          - not_null
          - unique
      - name: customer_id
        description: Customer identifier for the visit.
        data_tests:
          - not_null
      - name: visit_date
        description: Date of the park visit.
        data_tests:
          - not_null
      - name: ticket_type
        description: Ticket product type used for the visit.
      - name: purchase_date
        description: Date the ticket was purchased.
      - name: purchase_channel
        description: Ticket purchase channel for the visit.
      - name: is_discounted
        description: TRUE when the ticket had a non-zero discount amount.
      - name: ticket_price
        description: Ticket revenue attributed to the visit.
      - name: in_park_spend
        description: POS spend matched to the customer and visit date.
      - name: total_visit_spend
        description: "Combined visit spend: `ticket_price + in_park_spend`."

      - name: booking_lead_days
        description: Days between ticket purchase and visit date.
      - name: is_same_day_visit
        description: TRUE when the ticket was purchased on the visit date.
      - name: is_advance_purchase
        description: TRUE when the ticket was purchased at least 7 days before the visit.
      - name: avg_rating
        description: Average matched visitor rating for the visit. NULL means no feedback.
      - name: feedback_count
        description: Number of matched feedback submissions for the visit.
      - name: has_feedback
        description: TRUE when at least one matched feedback row exists for the visit.

unit_tests:
  - name: test_daily_revenue_ticket_only_date
    model: int_daily_revenue
    given:
      - input: ref('stg_sales__tickets')
        rows:
          - {ticket_id: 'TK1', customer_id: 'C1', visit_date: '2024-06-01', final_price: 80.00}
          - {ticket_id: 'TK2', customer_id: 'C2', visit_date: '2024-06-01', final_price: 40.00}
      - input: ref('stg_sales_transactions__sales_transactions')
        rows: []
    expect:
      rows:
        - {visit_date: '2024-06-01', ticket_revenue: 120.00, in_park_revenue: 0, food_revenue: 0, merch_revenue: 0, total_revenue: 120.00, unique_visitors: 2, tickets_sold: 2}

  - name: test_daily_revenue_sales_only_date
    model: int_daily_revenue
    given:
      - input: ref('stg_sales__tickets')
        rows: []
      - input: ref('stg_sales_transactions__sales_transactions')
        rows:
          - {transaction_id: 'TX1', customer_id: 'C1', visit_date: '2024-06-02', category: 'food', total_amount: 25.00}
    expect:
      rows:
        - {visit_date: '2024-06-02', ticket_revenue: 0, in_park_revenue: 25.00, food_revenue: 25.00, merch_revenue: 0, total_revenue: 25.00, unique_visitors: 0, tickets_sold: 0}

  - name: test_daily_revenue_category_splitting
    model: int_daily_revenue
    given:
      - input: ref('stg_sales__tickets')
        rows:
          - {ticket_id: 'TK1', customer_id: 'C1', visit_date: '2024-06-03', final_price: 60.00}
      - input: ref('stg_sales_transactions__sales_transactions')
        rows:
          - {transaction_id: 'TX1', customer_id: 'C1', visit_date: '2024-06-03', category: 'food', total_amount: 30.00}
          - {transaction_id: 'TX2', customer_id: 'C1', visit_date: '2024-06-03', category: 'merchandise', total_amount: 20.00}
          - {transaction_id: 'TX3', customer_id: 'C1', visit_date: '2024-06-03', category: 'game', total_amount: 10.00}
    expect:
      rows:
        - {visit_date: '2024-06-03', ticket_revenue: 60.00, in_park_revenue: 60.00, food_revenue: 30.00, merch_revenue: 20.00, total_revenue: 120.00, unique_visitors: 1, tickets_sold: 1}

  - name: test_ride_metrics_no_reviews
    model: int_ride_metrics
    given:
      - input: ref('stg_park_assets__rides')
        rows:
          - {ride_id: 1, ride_name: 'Haunt Coaster', ride_type: 'coaster', thrill_level: 'high', zone: 'north', is_haunted: true, capacity_per_hour: 400, avg_wait_minutes: 20}
      - input: ref('stg_feedback__visitor_feedback')
        rows: []
    expect:
      rows:
        - {ride_id: 1, total_reviews: 0, avg_rating: 0, positive_reviews: 0, negative_reviews: 0, positive_review_pct: 0}

  - name: test_ride_metrics_review_aggregation
    model: int_ride_metrics
    given:
      - input: ref('stg_park_assets__rides')
        rows:
          - {ride_id: 2, ride_name: 'Ghost Drop', ride_type: 'drop_tower', thrill_level: 'extreme', zone: 'east', is_haunted: true, capacity_per_hour: 250, avg_wait_minutes: 35}
      - input: ref('stg_feedback__visitor_feedback')
        rows:
          - {feedback_id: 'F1', ride_id: 2, rating: 5, sentiment: 'positive', category: 'ride', customer_id: 'C1', visit_date: '2024-01-01'}
          - {feedback_id: 'F2', ride_id: 2, rating: 4, sentiment: 'positive', category: 'ride', customer_id: 'C2', visit_date: '2024-01-01'}
          - {feedback_id: 'F3', ride_id: 2, rating: 3, sentiment: 'neutral', category: 'ride', customer_id: 'C3', visit_date: '2024-01-01'}
          - {feedback_id: 'F4', ride_id: 2, rating: 4, sentiment: 'negative', category: 'ride', customer_id: 'C4', visit_date: '2024-01-01'}
    expect:
      rows:
        - {ride_id: 2, total_reviews: 4, avg_rating: 4.0, positive_reviews: 3, negative_reviews: 0, positive_review_pct: 75.00}



  - name: test_ride_metrics_ignores_null_ride_feedback
    model: int_ride_metrics
    given:
      - input: ref('stg_park_assets__rides')
        rows:
          - {ride_id: 3, ride_name: 'Tunnel Terror', ride_type: 'dark_ride', thrill_level: 'medium', zone: 'west', is_haunted: true, capacity_per_hour: 200, avg_wait_minutes: 15}
      - input: ref('stg_feedback__visitor_feedback')
        rows:
          - {feedback_id: 'F1', ride_id: 3, rating: 5, sentiment: 'positive', category: 'ride', customer_id: 'C1', visit_date: '2024-01-01'}
          - {feedback_id: 'F2', ride_id: null, rating: 1, sentiment: 'negative', category: 'general', customer_id: 'C2', visit_date: '2024-01-01'}
          - {feedback_id: 'F3', ride_id: null, rating: 2, sentiment: 'negative', category: 'general', customer_id: 'C3', visit_date: '2024-01-01'}
    expect:
      rows:
        - {ride_id: 3, total_reviews: 1, positive_reviews: 1, negative_reviews: 0, positive_review_pct: 100.00}


  - name: test_customer_visit_no_inpark_spend
    model: int_customer_visits
    given:
      - input: ref('stg_sales__tickets')
        rows:
          - {ticket_id: 'TK1', customer_id: 'C1', visit_date: '2024-07-01', purchase_date: '2024-07-01', purchase_channel: 'online', is_discounted: false, ticket_type: 'day_pass', final_price: 55.00}
      - input: ref('stg_sales_transactions__sales_transactions')
        rows: []
      - input: ref('stg_feedback__visitor_feedback')
        rows: []
    expect:
      rows:
        - {ticket_id: 'TK1', customer_id: 'C1', in_park_spend: 0, total_visit_spend: 55.00, booking_lead_days: 0, is_same_day_visit: true, is_advance_purchase: false, feedback_count: 0, has_feedback: false, avg_rating: null}

  - name: test_customer_visit_with_spend_and_feedback
    model: int_customer_visits
    given:
      - input: ref('stg_sales__tickets')
        rows:
          - {ticket_id: 'TK2', customer_id: 'C2', visit_date: '2024-07-10', purchase_date: '2024-07-01', purchase_channel: 'online', is_discounted: true, ticket_type: 'weekend_pass', final_price: 80.00}
      - input: ref('stg_sales_transactions__sales_transactions')
        rows:
          - {transaction_id: 'TX1', customer_id: 'C2', visit_date: '2024-07-10', total_amount: 35.00}
          - {transaction_id: 'TX2', customer_id: 'C2', visit_date: '2024-07-10', total_amount: 25.00}
      - input: ref('stg_feedback__visitor_feedback')
        rows:
          - {feedback_id: 'F1', customer_id: 'C2', visit_date: '2024-07-10', rating: 4}
          - {feedback_id: 'F2', customer_id: 'C2', visit_date: '2024-07-10', rating: 5}
    expect:
      rows:
        - {ticket_id: 'TK2', in_park_spend: 60.00, total_visit_spend: 140.00, booking_lead_days: 9, is_same_day_visit: false, is_advance_purchase: true, feedback_count: 2, has_feedback: true}

